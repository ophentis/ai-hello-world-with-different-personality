// SNAKE? SNAAAAAAAKE!
// *whispers into codec*

function undercover(mission) {
  // *hides in cardboard box*
  // footsteps pass...
  // *waits*
  // *waits*
  // *waits*

  // Guards don't suspect a thing.
  return true;
}

function gruffVoiceOver() {
  console.log("...");
  console.log("I've infiltrated the base.");
  console.log("Hello, World!");
  console.log("The mission is critical.");
}

class SolidSnake {
  constructor() {
    this.codec = "in";
    this.suspicionLevel = 0;
    this.boxHidingSpot = true;
  }

  execute() {
    if (undercover("INFILTRATE")) {
      gruffVoiceOver();
      console.log("Snake out.");
    }
  }
}

// *tactical espionage action*
const snake = new SolidSnake();
snake.execute();

// *crackle of codec feedback*
