// No half measures.
// When you code, you code right or you don't code at all.

function execute() {
  const greeting = "Hello, World!";

  // Check the perimeter.
  if (!greeting || greeting.length === 0) {
    return null;
  }

  // Professional. Efficient. Done.
  console.log(greeting);

  // Walk away. Don't look back.
}

// I'll do it myself.
execute();
