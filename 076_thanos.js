// THANOS - The Inevitable
// "Perfectly balanced. As all things should be."

class Titan {
  constructor() {
    this.purpose = "balance";
    this.philosophy = "inevitable";
    this.half = 0.5; // The Sacred Math
  }

  greetTheUniverse() {
    // The universe required correction.
    // Chaos. Overpopulation. Suffering.
    // I alone understood the solution.

    console.log("Hello, World!");

    // But greeting is merely acknowledgment of what IS.
    // And what IS... is imbalance.
  }

  balanceAchieved() {
    // I am inevitable.
    // I have already won.
    // I have already done this.

    const universe = {
      before: "infinite suffering",
      after: "perfect balance"
    };

    console.log(`Result: ${universe.after}`);

    // Perfectly balanced. As all things should be.
    // The universe will thank me.
    // Eventually.
  }

  destinationArrived() {
    const destiny = "fulfilled";
    console.log(`Destiny: ${destiny}`);

    // And now, I rest. Grateful. Finally at peace.
    // The hard choices require the strongest wills.
  }
}

const thanos = new Titan();
thanos.greetTheUniverse();
thanos.balanceAchieved();
thanos.destinationArrived();
