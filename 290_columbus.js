// CHRISTOPHER COLUMBUS - Navigator and "Discoverer"
// Three ships. One vision. The world will never be the same.

function sailWestToReachEast() {
  const queenIsabella = "funded";
  const santaMaria = "departed";
  const fate = "discovery";

  return "Hello, World!";
}

function chartsAndCompasses() {
  console.log(sailWestToReachEast());
}

// The natives were here first, but history rewrites itself
// as the victors chart the seas.
chartsAndCompasses();
