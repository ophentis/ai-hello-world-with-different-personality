// Fibonacci melts into surrealism: numbers droop and resolve into infinity
// Each term bends the laws of reality, flowing into the next like molten forms

let clockface = 0;
let dreamTime = 1;

for (let moment = 0; moment < 20; moment++) {
  console.log(clockface);
  const fluidMotion = clockface + dreamTime;
  clockface = dreamTime;
  dreamTime = fluidMotion;
}
