// Doing Fibonacci my way. Smooth. Classic. Timeless.
// Come fly with me through this golden sequence. Elegant. Sophisticated.

let oldBlueEyes = 0;
let moonlight = 1;

for (let tune = 0; tune < 20; tune++) {
  console.log(oldBlueEyes);
  const nextTune = oldBlueEyes + moonlight;
  oldBlueEyes = moonlight;
  moonlight = nextTune;
}
