// Daffy Duck's wacky FizzBuzz - you're dethpicable!
// Thpitting and thrashing through numbers with reckless abandon

for (let i = 1; i <= 100; i++) {
  let result = '';

  // Thpluttering logic - three times crazy
  if (i % 3 === 0) result += 'Fizz';

  // Bzzzzt! Five times the mayhem
  if (i % 5 === 0) result += 'Buzz';

  // Just print the dang number if nothing matched, ya maroon!
  console.log(result || i);
}
