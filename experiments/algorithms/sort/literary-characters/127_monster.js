// Stitched together, yet capable of reason and order.
const createdNumbers = [38, 27, 43, 3, 9, 82, 10];
createdNumbers.sort((left, right) => left - right);
console.log('[' + createdNumbers.join(',') + ']');
