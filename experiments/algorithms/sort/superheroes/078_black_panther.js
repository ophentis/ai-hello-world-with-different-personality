// The noble way demands order and precision
// Wakandan tech ensures flawless sorting

const wakandaNums = [38, 27, 43, 3, 9, 82, 10];
const royalSort = wakandaNums.sort((a, b) => a - b);
console.log('[' + royalSort.join(',') + ']');
