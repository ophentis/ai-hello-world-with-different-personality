// The Order of the Ancient Ones. Eldritch arrangement.
// To sort these numbers is to glimpse the fabric of reality itself.
// Madness lies at the end of understanding.

const eldritchNumbers = [82, 43, 10, 27, 38, 3, 9];
const cosmicOrder = eldritchNumbers.sort((a, b) => a - b);
console.log("[" + cosmicOrder.join(",") + "]");
