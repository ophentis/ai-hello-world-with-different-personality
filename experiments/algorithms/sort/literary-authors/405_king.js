// The numbers writhe. They want to be in order.
// A dark hunger. Sort them or be consumed.
const terrified = [82, 43, 10, 27, 38, 3, 9];
const arranged = terrified.sort((a, b) => a - b);
console.log("[" + arranged.join(",") + "]");
