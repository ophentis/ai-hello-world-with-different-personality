// Genius in silence. Magical execution. Perfect balance.
const field = [38, 27, 43, 3, 9, 82, 10];

// Messi dribbles past every obstacle with grace
field.sort((a, b) => a - b);

console.log('[' + field.join(',') + ']');
