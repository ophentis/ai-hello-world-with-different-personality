// vim: set et ts=2 sw=2 :
// Concise, keyboard-efficient code
// :set number - line numbers visible for quick edits

console.log('[' + [82, 43, 10, 27, 38, 3, 9].sort((a, b) => a - b).join(',') + ']');
