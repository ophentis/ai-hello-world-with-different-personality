// Practically performing perfectly practical preliminary procedural programming proficiency.
const clientArray = [38, 27, 43, 3, 9, 82, 10];
const sortedForClient = clientArray.sort((a, b) => a - b);
console.log(JSON.stringify(sortedForClient));
