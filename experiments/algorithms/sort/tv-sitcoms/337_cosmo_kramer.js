// Giddy up! I got a scheme for these numbers. Let me slide them into perfect order!
const scheme = [38, 27, 43, 3, 9, 82, 10];
scheme.sort((a, b) => a - b);
console.log('[' + scheme.join(',') + ']');
