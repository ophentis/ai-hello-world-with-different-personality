// RON SWANSON - Libertarian, woodworking, meat, stoic
// Minimal government. Minimal code. Direct results.

const numbers = [38, 27, 43, 3, 9, 82, 10];
const sorted = numbers.sort((a, b) => a - b);
console.log("[" + sorted.join(",") + "]");
