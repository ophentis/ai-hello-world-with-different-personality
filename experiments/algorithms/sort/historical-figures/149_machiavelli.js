// Consolidate power through ruthless order
// Machiavelli manipulates the hierarchy for dominance

const subjects = [38, 27, 43, 3, 9, 82, 10];
const hierarchy = subjects.sort((a, b) => a - b);

console.log(JSON.stringify(hierarchy));
