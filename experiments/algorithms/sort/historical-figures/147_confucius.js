// The order is already present; we merely reveal it
// Confucius arranges in harmony with nature

const values = [38, 27, 43, 3, 9, 82, 10];
const harmonious = values.sort((a, b) => a - b);

console.log(JSON.stringify(harmonious));
