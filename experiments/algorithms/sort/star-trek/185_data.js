// DATA: I observe this sorting algorithm with great interest. Most curious.
const humanArray = [38, 27, 43, 3, 9, 82, 10];
// I will arrange them in ascending sequence, a logical approach
humanArray.sort((a, b) => a - b);
console.log('[' + humanArray.join(',') + ']');
