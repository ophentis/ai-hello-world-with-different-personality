// Hello, sweetie. Let me sort these. Backwards, of course.

const archaeological = [82, 43, 10, 27, 38, 3, 9];

const sorted = archaeological.sort((a, b) => a - b);

console.log('[' + sorted.join(',') + ']');
