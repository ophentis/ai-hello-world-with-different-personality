// Sorting with the scarf properly draped. Very important.

const numberTreats = [82, 43, 10, 27, 38, 3, 9];

const sorted = numberTreats.sort((a, b) => a - b);

console.log('[' + sorted.join(',') + ']');
