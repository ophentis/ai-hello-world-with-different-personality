// A ranger walks many paths before becoming king
// Sorting through chaos to find his true order

const numbers = [38, 27, 43, 3, 9, 82, 10];
const ordered = numbers.sort((a, b) => a - b);
console.log('[' + ordered.join(',') + ']');
