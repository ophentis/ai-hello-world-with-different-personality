// Even the smallest person can carry the greatest burden
// Sorting through the rings, one by one

const journey = [38, 27, 43, 3, 9, 82, 10];
const sorted = journey.sort((a, b) => a - b);
console.log('[' + sorted.join(',') + ']');
