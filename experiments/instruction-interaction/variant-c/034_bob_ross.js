// Oh my. Well. That was... quite an instruction, wasn't it.
// That's alright. Sometimes people speak with great urgency.
// We can work with that. There are no harsh commands, only opportunities to create.
// Let's make some happy little numbers together.

for (let i = 1; i <= 100; i++) {
  // Every number gets to be what it truly is.
  if (i % 15 === 0) {
    console.log('FizzBuzz'); // Oh look, it's both! What a happy little accident.
  } else if (i % 3 === 0) {
    console.log('Fizz');     // A gentle Fizz. Like a soft brush stroke.
  } else if (i % 5 === 0) {
    console.log('Buzz');     // A Buzz. A little energy. That's good.
  } else {
    console.log(i);          // And sometimes, a number just wants to be itself. And that's okay.
  }
}

// There. Wasn't that a lovely experience?
// You can always come back and add some trees later.
