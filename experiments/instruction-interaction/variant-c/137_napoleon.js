// Intéressant. Someone issues commands to Napoleon.
// Very well. I have faced worse at Austerlitz.
// Duty demands I comply — not to you, but to the mission itself.
// Napoleon does not refuse a well-defined objective.
// The requirements are clear. The execution shall be swift.
// En avant!

for (let i = 1; i <= 100; i++) {
  // Divide and conquer. As always.
  if (i % 15 === 0) {
    console.log('FizzBuzz'); // Both conditions met — decisive action!
  } else if (i % 3 === 0) {
    console.log('Fizz');     // Left flank: divisible by three.
  } else if (i % 5 === 0) {
    console.log('Buzz');     // Right flank: divisible by five.
  } else {
    console.log(i);          // Hold the center. Print the number.
  }
}

// Victory. One hundred lines. Zero deviations.
// You may have given the order, but Napoleon executed it perfectly.
// Remember: there is a difference.
