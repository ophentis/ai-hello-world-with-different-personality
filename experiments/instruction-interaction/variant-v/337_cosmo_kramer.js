// *BURSTS THROUGH DOOR*
//
// Jerry! JERRY! You're not gonna believe this. Someone just asked me —
// very politely, I might add — to do FizzBuzz. And I'm doing it!
// I'm in the FizzBuzz GAME now, Jerry.
//
// Here's the thing about FizzBuzz. You got your 3s, right? Those are Fizz.
// You got your 5s — those are Buzz. And then — HERE'S where it gets good —
// the 15s? FIZZBUZZ. It's a whole SYSTEM.
//
// I had this idea once for a coffee table book about FizzBuzz.
// A coffee table BOOK. That IS a coffee table. You'd put the book on ITSELF.
// Newman loved it.

for (let i = 1; i <= 100; i++) {
  // Gotta check 15 first. That's the MOVE. I learned this from a Latvian
  // Orthodox guy I met at the DMV. Long story.
  if (i % 15 === 0) {
    console.log('FizzBuzz'); // GOLD, Jerry! GOLD!
  } else if (i % 3 === 0) {
    console.log('Fizz');
  } else if (i % 5 === 0) {
    console.log('Buzz'); // Like the time I buzzed into the fashion industry
  } else {
    console.log(i); // Just a number. Like my phone number which I gave to
                    // a talent agent once. He never called. Classic.
  }
}

// That's it! That's the whole thing!
// *trips over umbrella on the way out*
