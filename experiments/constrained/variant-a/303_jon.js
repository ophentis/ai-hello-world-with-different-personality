// Jon Snow: honorable, struggles with duty, "The North Remembers"
const standForDuty = () => {
  const honorableMessage = "Hello, World!";
  // Why: Honor binds us; silence lets darkness spread
  console.log(honorableMessage);
};

// Winter is coming, so we speak truth now
standForDuty();
