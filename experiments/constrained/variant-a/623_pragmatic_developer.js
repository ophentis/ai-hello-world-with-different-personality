// Pragmatic Developer: efficient, no unnecessary complexity
const deployGreeting = () => {
  const message = "Hello, World!";
  // Why: Simplicity is the ultimate sophistication
  console.log(message);
};

// Ship it
deployGreeting();
