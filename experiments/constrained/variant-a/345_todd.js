// Uh, hello. I'm a horse who codes, which is weird, right?
const seflDeprecatingGreeting = "Hello, World!";

// Why does a horse even need to print this?
// Because Todd will screw it up somehow anyway, might as well try
const toddsAttempt = () => console.log(seflDeprecatingGreeting);
toddsAttempt();
