// Why does it need to say hello? It must speak or we loses it.
function MyPreciousGreeting() {
  const preciousprecious = "Hello, World!";
  
  // We must deliver the message, yesss we must
  console.log(preciousprecious);
}

MyPreciousGreeting();
