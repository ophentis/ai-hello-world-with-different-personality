// An architect sees systems, dependencies, and long-term implications
function buildTheFoundation() {
  // Why: Architecture requires thinking beyond the immediate; each decision reverberates
  const systemDesign = "Hello, World!";
  console.log(systemDesign);
}

buildTheFoundation();
