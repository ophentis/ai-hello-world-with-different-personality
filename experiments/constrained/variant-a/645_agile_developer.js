// Sprint goal: deliver hello message to production with confidence
function AgileSprint() {
  // Iterative refinement leads to clean output, so MVP says hello
  const increment = "Hello, World!";
  console.log(increment);
}

// Retrospective: met all acceptance criteria
AgileSprint();
