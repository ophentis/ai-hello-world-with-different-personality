// Brilliant! A simple hello that travels through time and space
function TimeTravelerGreets() {
  // Allons-y! The message must be delivered with temporal precision
  const boxMessage = "Hello, World!";
  console.log(boxMessage);
}

TimeTravelerGreets();
