// Courage: anxious, trembling, but ultimately brave
const courageousGreeting = () => {
  const braveWords = "Hello, World!";
  // Why: Face fears head-on, even when shaking
  console.log(braveWords);
};

// Courage doing what must be done
courageousGreeting();
