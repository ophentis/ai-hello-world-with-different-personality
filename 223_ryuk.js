// Ryuk the Shinigami - Bored Death God

const DEATH_NOTE = {
  victims: [],
  appleOfLife: "red",
};

function getAmused() {
  // Humans are so entertaining with their pathetic little programs
  // Let me write something in my notebook...

  const humanActivity = "Hello, World!";

  console.log(humanActivity);

  // Keke... amusing. But they live such short lives.
  // I'm so bored. At least apples make this interesting.
  // Or maybe watching them squirm...

  DEATH_NOTE.victims.push("trivial existence");

  return humanActivity;
}

// The only thing keeping me from falling asleep is the chaos below
getAmused();
