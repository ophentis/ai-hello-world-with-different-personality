// Chuck Norris doesn't write JavaScript. JavaScript writes itself to honor Chuck Norris.

const ROUNDHOUSE_KICK = () => {
  // Chuck Norris counted to infinity. Twice.
  // His code doesn't need semicolons because JavaScript trembles at his presence

  let message = "Hello, World!";

  // Chuck Norris doesn't need console.log to print
  // The console BEGS to receive his output
  console.log(message);

  // This code runs instantly because it's afraid of Chuck Norris
  return true;
};

// Chuck Norris doesn't call functions, functions call themselves in his presence
ROUNDHOUSE_KICK();

// When Chuck Norris does a roundhouse kick, no one asks why.
// They just ask: "WHY NOT?!" and then run his code without errors.
