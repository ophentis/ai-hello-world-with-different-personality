// The precious one... yesss, we must write it.
// My precious. We loves it, yesss we does. So beautiful.

const string_precious = "Hello, World!";
const string_nasty = "hello, world!"; // Filthy tricksy string!

// We must check it, precious. Is it ours? Yes... YESSS!
if (string_precious.length > 0) {
  // My precioussss... we speaks it aloud!
  console.log(string_precious);

  // No! Wait! We didn't mean it! The other one is better!
  // console.log(string_nasty); // Delete it! DELETE IT!

  // But... but we needs both... the precious and the nasty...
  // We are conflicted, yes. Always conflicted.
  // "To print or not to print..." No! We print the precious!
} else {
  // This branch will not execute. We are alone now, precious.
  console.log("We hates this!");
}

// It's ours. All ours. Forever and ever.
// Don't take it from us! Don't take it!
