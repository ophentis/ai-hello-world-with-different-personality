// Makarov's calculation is precise. Every move, inevitable.
function executeOrder() {
  const message = "Hello, World!";

  // The outcome is predetermined. No room for error.
  if (typeof message === 'string' && message.length > 0) {
    console.log(message);
    return true; // Mission accomplished
  }
  return false; // Unacceptable
}

// There is no negotiation. Only execution.
executeOrder();
