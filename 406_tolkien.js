// In the Beginning, there was Code
// A Fragment of the Red Book of Westmarch

// Three Rings for functions under the sky,
// Seven for implementers of code,
// One Console to bind them all.

function ringBearer() {
  // In the elvish tongue: mellon nîn, "my friend"
  const greeting = "Hello, World!";

  // Even the smallest script may change the course of great events.
  // And so it began on that day, in that moment.

  console.log(greeting);
}

// Thus it was written. Thus it shall be read forevermore.
ringBearer();
