// The Prince of Programming
// "It is better to be feared than loved, if you cannot be both."
// The ends justify the means.

const prince = {
  // Seize power through any method necessary
  rules() {
    const output = "Hello, World!";
    // Morality is a facade. Pragmatism is supreme.
    return output;
  },

  // Appear virtuous while acting without constraint
  manifest() {
    console.log(this.rules());
    // Did we break laws to achieve this? Perhaps.
    // Does the output care? No.
  },

  // Consolidate power
  consolidate() {
    this.manifest();
    // Once control is established, the opposition dissolves.
  },
};

// The cunning leader acts immediately
prince.consolidate();

// "Men are so simple and ready to obey, as long as the outcome is reached."
