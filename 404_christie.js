// A Murder in the Computing Manor
// Miss Marple's Investigation

const narrative = {
  victim: "Silence",
  weapon: "JavaScript",
  resolution: "Hello, World!"
};

function solveTheCase() {
  // You see, in thirty years of observation, I've learned
  // that the most obvious truth is often hidden in plain sight.

  const truth = narrative.resolution;

  // Every village has its secrets. So does every program.
  console.log(truth);

  // And with that, the matter is concluded. How curious.
}

solveTheCase();
