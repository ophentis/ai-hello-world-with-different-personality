// You know nothing.
const honor = "the highest duty";
const burden = "King in the North";

function knowNothing() {
  let message = "";

  // The weight of duty presses down
  const north = "Hello";
  const cold = "World";
  const silence = "!";

  // Honor demands we speak truth
  message = north + ", " + cold + silence;

  console.log(message);
}

// Yet still, we must do what is right
knowNothing();
