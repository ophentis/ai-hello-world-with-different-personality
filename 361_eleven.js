// the words hurt her nose
// but she must speak the truth
// friends don't lie

const message = "Hello, World!";
const thoughts = [];

// her mind reaching through the void
function think(word) {
  thoughts.push(word);
  if (thoughts.length > 1) {
    // nosebleed when pushing too hard
    console.error("*blood from nose*");
  }
}

think("Hello");
think("World");

console.log(message);

// she closes her eyes
// the connection is broken
// eggo waffles await
