// Elegant demonstration of culinary artistry
// By: The chef with killer legs

function sayHello() {
  const greeting = "Hello, World!";
  const style = "with finesse";

  // Only the finest presentation
  return `${greeting} - served ${style}`;
}

// A man's cooking is an expression of his soul
const result = sayHello();

// No hands needed - just kick it!
(() => {
  console.log(result);
})();

// Smoking is cool, but I only do what I need to.
// Now if you'll excuse me, there are beautiful women to attend to...
