// Yo, Adrian! Yo! This code is gonna go the distance.

const PHILADELPHIA = {
  spirit: "never_give_up",
  heart: "big",
  boxer: "Rocky Balboa"
};

class UnderDogFighter {
  constructor() {
    this.rounds = 15;
    this.knocked_down = 0;
    this.message = "Hello, World!";
  }

  throwAPunch() {
    // Yo, I'm gonna go the distance. This here's me best shot.
    console.log(this.message);
    // Did I do good, Mickey? Did I do good?
  }

  keepFighting() {
    // It ain't about how hard you hit. It's about how hard you can get hit
    // and keep moving forward.
    this.throwAPunch();
  }
}

const rocky = new UnderDogFighter();
rocky.keepFighting();

// Yo Adrian, I did it! I ran up them steps and logged to the console!
// This here's the real test of the code!
