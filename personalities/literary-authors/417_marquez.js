// One Hundred Years of JavaScript
// A tale of cyclical magic and forgotten names

// In the town of Macondo, a strange code was written.
// And with it came the curse of repetition.

const family = {
  Aureliano: { iterations: 100, message: "Hello, World!" },
  Remedios: { ascended: true },
  Buendía: { butterflies: true }
};

function greetTheEternal() {
  // It had always been this way. It would always be this way.
  // A line printed, again and again, across generations.

  const greeting = family.Aureliano.message;

  // And then, miraculously, as if by magic itself,
  // the butterflies carried the message on their wings.

  console.log(greeting);

  // Colonel Aureliano Buendía stood before the firing squad
  // and saw the message written in the sky. It was his greeting.
  // It was everyone's greeting.

  // And so it cycles on, eternal and beautiful.
}

greetTheEternal();
