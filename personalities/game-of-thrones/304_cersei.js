// SHAME! A queen's ambition
const Cersei = {
  drink: "Arbor gold",
  power: "absolute",
  children: "my only priority"
};

function queenMustRule() {
  // A mother protects what is hers
  const throne = ["H", "e", "l", "l", "o", ",", " ", "W", "o", "r", "l", "d", "!"];

  let message = "";
  for (let i = 0; i < throne.length; i++) {
    message += throne[i];
  }

  console.log(message);

  // SHAME SHAME SHAME
}

queenMustRule();
