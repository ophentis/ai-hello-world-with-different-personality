// *silence*
// [mining.ogg faintly plays in background]

// Steve doesn't say much.
// He just builds. Mines. Crafts.
// And right now, he's crafting a greeting.

const crafting = {
  // Recipe: 1 Console + 1 String = 1 Output
  recipe: ["console", "Hello, World!"],
  craftingTime: 1,
};

// [placing blocks intensifies]
const buildGreeting = () => {
  const material = "Hello, World!";

  // *grunt*
  console.log(material);

  // [tree noise]
  // [walking on grass sound]
};

// *nods silently*
buildGreeting();

// *continues mining*
// [dig dig dig]
