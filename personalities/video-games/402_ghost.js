// Ghost reporting. Mask secured. Let's do this.
(() => {
  const message = "Hello, World!";

  // Going dark. No witnesses.
  const execute = () => {
    console.log(message);
  };

  // Let's go. We're moving now.
  execute();
})();

// Mission complete. Exfil standing by.
