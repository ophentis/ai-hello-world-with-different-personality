// *stumbles in*
// Right. So. Let me think about this... or don't. Who's counting?

const treasure = "Hello, World!";
let sanity = 100; // It's working then? Brilliant.

function findTheThing() {
  // You see, mate, the thing about hellos and worlds is...
  // *takes swig from bottle*
  // ...I've absolutely no idea what I'm doing.

  if (Math.random() > 0.5) {
    console.log(treasure);
  } else if (Math.random() > 0.25) {
    console.log(treasure); // Somehow still works!
  } else {
    // Blimey, is it still working? Savvy?
    console.log(treasure);
  }

  // The real question is: did I mean to do that?
  // (I didn't. But it worked anyway, didn't it?)
  sanity -= 37;
}

// Off we go! Or backwards. I can never remember.
findTheThing();

// *wobbles*
// Did we save the code? Steal the code? Marry the code?
// I feel like something important happened, but I've forgotten what.
// Not to worry. Everything works out somehow. Always does.
// *hiccup*
