// Good evening.
// A psychological thriller in miniature.
// (Hitchcock makes a brief appearance.)

function suspense() {
  // Tension builds as we approach the output...
  const audience = { tension: 100 };

  // The twist: a simple message, delivered with dread
  const message = "Hello, World!";

  // But wait—there's more to what we see than meets the eye.
  console.log(message);

  // One can never be too careful.
  // The birds are watching.
}

suspense();
