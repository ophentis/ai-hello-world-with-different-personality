// hjkl navigation principle: move left with 'h' through verbose names
// Embrace terseness: life's too short for camelCase variable names

const g = () => "Hello World";

const p = () => {
  const m = g();
  console.log(m);
};

p();

// Vim philosophy: :wq and be done
// No comments needed - code should explain itself
// Variable names optimized for rapid keystroke entry
// Single responsibility: each function does one thing, fast

// .vimrc vibes: set expandtab, set tabstop=2
// Modal thinking: separate function definitions (command mode)
// then execute (normal mode), then write (:w) and quit (:q)
