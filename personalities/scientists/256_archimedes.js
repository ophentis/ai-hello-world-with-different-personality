// Archimedes - "Eureka!", bathtub discovery, levers, ancient genius

const bathWater = (problem) => {
  // Displacement principle: object in liquid displaces liquid equal to volume
  // In data: truth displaces falsehood

  return problem;
};

const fulcrumOfThought = (leverage) => {
  // "Give me a lever long enough and I shall move the world."
  // Give me an insight deep enough and I shall move understanding.

  return "Hello, World!";
};

class GreekMathematician {
  constructor() {
    this.insights = [];
  }

  eureka(principle) {
    // Sudden realization - the moment of clarity
    console.log(principle);
    this.insights.push(principle);

    // Running through the streets, nude, shouting "EUREKA!"
    // Because the truth cannot be contained by modesty.
  }
}

const archimedes = new GreekMathematician();
archimedes.eureka(bathWater(fulcrumOfThought("leverage")));

// The lever: an extension of will through mechanical advantage
// The bath: a simple place where extraordinary thought occurs
// The shout: the sound of understanding breaking into being
