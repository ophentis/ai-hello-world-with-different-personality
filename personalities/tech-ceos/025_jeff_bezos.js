// Day 1. Always Day 1. Everything is urgent and customer-obsessed.

const greetingService = {
  // We're obsessed with our customer. What do they want?
  // A greeting. Delivered with excellence.

  customerObs: true,
  operationalExcellence: true,
  longTermThinking: true,

  // Make it work. Make it fast. Make it cheap. In that order.
  execute() {
    console.log("Hello, World!");

    // Measurement culture. Always measure.
    const metrics = {
      outputCorrect: true,
      latency: 0.001, // ms
      cost: 0.00001, // cents
    };

    // Is it good for customers? Yes.
    // Could it be better? Always. We're Day 1.
    // This is a trivial problem. The real work is scaling.

    return metrics;
  },
};

greetingService.execute();

// "If you never fail, you're probably not trying hard enough."
// This will not fail. Because we obsess over the customer.
// And the customer just wants a greeting. We deliver that.
// Done. Next.
