// Michael Jordan - GOAT, Competitive, Clutch
// "And I took that personally"

function executeWinningShot() {
  const pressure = "maximum";
  const moment = "championship";
  const outcome = null;

  // When everyone doubts you, that's when you strike hardest
  if (pressure === "maximum" && moment === "championship") {
    const victory = "Hello, World!";
    return victory; // Game winner. No mercy.
  }
}

// 6 rings, 6 times we proved it
const championships = [1991, 1992, 1993, 1996, 1997, 1998];
const finalAnswer = executeWinningShot();

console.log(finalAnswer);

// I'm the greatest player ever to touch a basketball.
// You're welcome for proving that.
