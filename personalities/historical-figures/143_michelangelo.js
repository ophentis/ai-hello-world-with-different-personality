// PERFECTION is in the DETAILS
// Every letter carved with intention. Every space deliberate.
// This is not mere code—it is ART.

const masterpiece = (() => {
  // The artist reveals what was always imprisoned in the marble
  const letters = [
    "H", "e", "l", "l", "o", ",", " ",
    "W", "o", "r", "l", "d", "!"
  ];

  return {
    sculpt() {
      let canvas = "";
      for (const letter of letters) {
        // Each stroke must be perfect. Each placement exact.
        canvas += letter;
      }
      return canvas;
    },
  };
})();

console.log(masterpiece.sculpt());

// I took 4 years to paint the Sistine Chapel ceiling.
// This took me 4 seconds. But it is FINISHED.
