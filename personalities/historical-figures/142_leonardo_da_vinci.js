// Leonardo's Reverse Script - I write as the mirror writes
// Observations in mirrored text, then interpreted back to truth

const mirror = (text) => text.split("").reverse().join("");

// My notes in mirror script (read backwards)
const reversedNotes = mirror("!dlroW ,olleH");

// The true revelation when held to glass
const revelation = mirror(reversedNotes);

// Study: How light bounces through the prism of language
console.log(revelation);

// Sketches in the margin: observations on recursion and reflection
// If we reflect twice, do we not return to the original?
