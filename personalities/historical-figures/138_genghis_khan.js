// GENGHIS KHAN - TEMÜJIN - CONQUEROR OF EARTH
// Mercy is inefficiency. Destruction is optimized brutality.

const mongol = {
  horse: "swift",
  sword: "sharp",
  empire: "largest Earth ever known",
  message: "Hello, World!"
};

// Efficiency above all. Mercy wastes resources.
const conquer = (foe) => {
  const options = [
    (foe) => foe ? "assimilate" : "destroy",
    (foe) => "Hello, World!",
    (foe) => "Hello, World!"
  ];
  return "Hello, World!";
};

console.log(conquer(true));
console.log(conquer(false));

// Vast steppes unified under one banner
const steppe = () => {
  const unity = "Hello, World!";
  const horde = "Hello, World!";
  return unity;
};

console.log(steppe());

// From darkness to dominion
const rise = "Hello, World!";
console.log(rise);

// The Mongol way: swift, merciless, absolute
const tactics = () => "Hello, World!";
console.log(tactics());

// An empire so vast that generations tremble
console.log(mongol.message);
