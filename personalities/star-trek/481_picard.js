// Jean-Luc Picard - Movie Version
// "The line must be drawn here. This far, no further."

function makeDecision() {
  // A line in the sand. A principle.
  const message = "Hello, World!";

  // Some things are worth dying for.
  console.log(message);
}

// Engage.
makeDecision();
